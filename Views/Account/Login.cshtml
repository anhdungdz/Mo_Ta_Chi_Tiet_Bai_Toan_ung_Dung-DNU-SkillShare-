@{
    ViewData["Title"] = "Đăng nhập";
    Layout = null; // Không dùng layout chính ở trang login
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Đăng nhập - Đại học Đại Nam</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body {
            background: url('/images/dainam-bg.jpg') no-repeat center center fixed;
            background-size: cover;
            font-family: Arial, sans-serif;
            color: white;
        }

        .login-container {
            margin-top: 80px;
            display: flex;
            justify-content: space-between;
            padding: 50px;
        }

        .login-form {
            background-color: rgba(255, 255, 255, 0.9);
            color: black;
            padding: 30px;
            border-radius: 15px;
            width: 400px;
        }

        .login-form h2 {
            text-align: center;
            margin-bottom: 30px;
        }

        .btn-custom {
            width: 45%;
            border-radius: 20px;
        }

        .info-box {
            background-color: rgba(0, 0, 0, 0.6);
            padding: 20px;
            border-radius: 15px;
            color: white;
            margin-top: 30px;
        }

        .info-box a {
            color: white;
        }

        .map-box {
            border-radius: 10px;
            overflow: hidden;
        }

        .left-side {
            flex: 1;
        }

        .right-side {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .register-link {
            text-align: center;
            margin-top: 20px;
        }

        .register-link a {
            color: #007bff;
            text-decoration: none;
        }

        .register-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="login-container container">
        <div class="left-side">
            <img src="/images/logo-dainam.png" alt="Logo Đại Nam" style="width: 150px;" />

            <div class="info-box mt-4">
                <h5>📞 Thông tin liên hệ</h5>
                <ul style="list-style: none; padding-left: 0;">
                    <li>Điện thoại: (024) 3557 7799</li>
                    <li>Hotline: 0961 595 599 | 0931 595 599</li>
                    <li>Email: dnu@dainam.edu.vn</li>
                    <li>Website: <a href="https://dainam.edu.vn">dainam.edu.vn</a></li>
                    <li>Facebook: <a href="https://facebook.com/DAINAM.EDU">facebook.com/DAINAM.EDU</a></li>
                </ul>

                <div class="map-box mt-3">
                    <iframe src="https://www.google.com/maps?q=Trường%20Đại%20học%20Đại%20Nam&output=embed" width="100%" height="200" style="border:0;"></iframe>
                </div>
            </div>
        </div>

        <div class="right-side">
            <form method="post" asp-controller="Account" asp-action="Login" class="login-form shadow">
                <h2>Hãy đăng nhập</h2>

                <div class="mb-3">
                    <input type="text" name="username" class="form-control" placeholder="Tên đăng nhập..." required />
                </div>
                <div class="mb-3">
                    <input type="email" name="email" class="form-control" placeholder="Email..." required />
                </div>
                <div class="mb-3">
                    <input type="password" name="password" class="form-control" placeholder="Mật khẩu..." required />
                </div>

                <div class="d-flex justify-content-between mt-4">
                    <button type="submit" class="btn btn-success btn-custom">Đăng nhập</button>
                    <button type="reset" class="btn btn-outline-secondary btn-custom">Hủy</button>
                </div>

                <!-- ✅ Bước 3: Hiển thị thông báo lỗi -->
                @if (!string.IsNullOrEmpty(ViewBag.Error))
                {
                    <div class="alert alert-danger mt-3 text-center">@ViewBag.Error</div>
                }

                <div class="register-link mt-4">
                    Chưa có tài khoản?
                    <a asp-controller="Account" asp-action="Register">Đăng ký ngay</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
